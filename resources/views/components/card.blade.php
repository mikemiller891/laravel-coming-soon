<!-- Card -->
<div class="flex items-center p-4 bg-white rounded-lg shadow-sm">
    <div class="p-3 mr-4 rounded-full bg-red-100 text-red-500">
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
            <!-- TODO: Slot in an icon. -->
        </svg>
    </div>
    <div>
        <p class="mb-2 text-sm font-medium text-gray-600">{{ $caption }}</p>
        <p class="text-lg font-semibold text-gray-700">{{ $text }}</p>
    </div>
</div>
